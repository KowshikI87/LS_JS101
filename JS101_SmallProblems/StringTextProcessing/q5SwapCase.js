function swapCase(words) {
  //start a new string
  //go through each character
  //if it is >= 'a' and <= 'z' then concatenate uppercase of 
  //if it is >= 'A' and <=' Z' then concatenate lowercase of 
  //solution skipped as it matches with LS

}